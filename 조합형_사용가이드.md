# 조합형(3번 타입) — 사용 가이드

새 "조합형" 단어 외우기 세트를 만들고 미리보기하는 방법. 기획 배경은
`shorts_plan.md`, 현재 부족한 리소스·제약은 `조합형_need.md` 참고.

---

## 0. 한눈에 보는 구조

| 역할 | 파일 |
|---|---|
| 상태머신(타이밍 진행) | `studio/studios/word_memorize.py` |
| 조합형 전용 씬 렌더러(그림·애니메이션) | `studio/studios/word_memorize_compose.py` |
| 레이아웃 스키마(JSON 로드/저장) | `extra/table_editor/services/word_memorize_layout.py` |
| 단어 데이터 | `resource/table/words.xlsx` → `python -m tools.csv_gen` → `resource/csv/words.csv` |
| 배치(세트) 정의 | `resource/table/word_memorize_layouts/*.json` |
| 예시 세트 | `resource/table/word_memorize_layouts/조합_예시1.json` (书店/电灯/冰水) |

조합형은 `combo_layout: true`인 레이아웃 JSON만 있으면 동작한다. 박스의
`x/y/w/h`는 조합형 화면에서 **완전히 무시**된다(전용 씬이 자체 배치를 그림) —
의미 있는 건 `boxes` 배열의 **word_id 3개와 순서(order)** 뿐이다.

---

## 1. 새 세트 만들기 — 단계별

### 1) 조합 후보 고르기 (부품이 이미 words.xlsx에 있는 단어 우선)

부품 한자(예: 电+灯)가 이미 개별 단어로 등록돼 있으면 사진(`img_path`)·병음까지
바로 재사용되니 가장 빠르다. 아래 스크립트로 "부품 2글자가 모두 이미 단어로
존재하는 2글자 합성어"를 찾을 수 있다(이번 예시 3세트도 이렇게 찾음):

```python
import openpyxl
wb = openpyxl.load_workbook("resource/table/words.xlsx", read_only=True, data_only=True)
rows = []
for name in wb.sheetnames:
    ws = wb[name]
    header = [c.value for c in next(ws.iter_rows(max_row=1))]
    idx = {h: i for i, h in enumerate(header)}
    for r in list(ws.iter_rows(values_only=True))[1:]:
        if r[idx["id"]] is None:
            continue
        rows.append({k: r[idx[k]] for k in idx})

single = {r["word"]: r for r in rows if r["word"] and len(r["word"]) == 1}
for r in rows:
    w = r["word"] or ""
    if len(w) == 2 and w[0] in single and w[1] in single:
        print(w, r["meaning"], "<-", w[0], single[w[0]]["meaning"], "+", w[1], single[w[1]]["meaning"])
```

없으면 부품 한자를 새 단어 row로 추가(기존 단어장 편집 방식 그대로 — id/word/pinyin/meaning).

**등급 기준(shorts_plan.md §5)**: 직역이 웃기거나 뜻밖인 조합(S급: 电脑=전기뇌=컴퓨터)이
가장 좋고, 자연스럽게 납득되는 조합(A급)도 무난함. 한 편에 3세트 구성.

### 2) 부품 연결 + 레이아웃 JSON — 자동화 도구 사용 (`build_compose_layout.py`)

이제 xlsx를 손으로 열 필요 없이 전용 스크립트로 한 번에 처리한다. 단, **부품·결과
단어가 words.xlsx에 이미 있어야** 한다 — 없으면 먼저 평소처럼 `.words_add` +
`python -m tools.words_batch.add_words`로 추가.

`.words_compose` 파일(3열, 탭 구분: `result  component1  component2`, 전부 한자
텍스트로 참조):

```
# result	component1	component2
书店	书	店
电灯	电	灯
冰水	冰	水
```

```bash
# 먼저 --dry-run으로 확인 (저장 없이 layout JSON 미리보기만 콘솔에 출력)
python -m tools.words_batch.build_compose_layout tools/words_batch/서점세트.words_compose \
    --like resource/table/word_memorize_layouts/조합_예시1.json --name 조합_서점세트 --dry-run

# 문제없으면 실제 실행 — words.xlsx의 component1_id/2_id 채움 + CSV 재생성 +
# combo_layout:true 레이아웃 JSON 저장까지 한 번에
python -m tools.words_batch.build_compose_layout tools/words_batch/서점세트.words_compose \
    --like resource/table/word_memorize_layouts/조합_예시1.json --name 조합_서점세트
```

한 세트는 정확히 3항목(B1/B2/B3). 부품·결과 중 words.xlsx에 없는 단어가 있으면
어떤 단어가 없는지 에러로 알려준다. 자세한 절차는 `.claude/skills/word-memorize-batch/SKILL.md` §4 참고.

### 3) 미리보기

```
run_table_editor.bat
```
(또는 `python -m extra.table_editor`)

메뉴에서 **"단어 외우기"** 선택 → 파일명 콤보박스에서 방금 만든 JSON 선택 →
- 모드: **"미리보기 (F5 debug)"**
- 뜻 언어: 한국어/영어/중국어
- **퀴즈 모드 체크는 조합형에 영향 없음** (항상 자동으로 우회됨 — 아무거나 선택해도 됨)

→ 실행하면 A(미리보기, 4초) → B1 → B2 → B3(각 10초, 트레이 누적) → A(복습, 4초)
순서로 재생된다. 각 B 안에서 **부품1 → 부품2 → 결과 단어** 순서로, 각각
"한국어 뜻 → (0.8초 겹쳐) 중국어 단어" TTS가 재생된다(다른 타입의 ko 모드와 동일한
순서·겹침). 앞 항목 TTS가 안 끝났으면 다음 항목은 그만큼 기다렸다가 재생(겹쳐서
동시에 말하지 않음) — 부품 뜻이 길면 결과 단어 내레이션이 임팩트 순간보다 늦게
시작될 수 있다. 해당 단어의 TTS가 없으면 조용히 건너뛰고 애니메이션은 그대로
진행됨 — `tools/tts_gen/build_word_memorize_tts.py`로 생성.

---

## 2. 타이밍·색·문구 커스터마이징

전부 `studio/studios/word_memorize_compose.py` 상단 상수에 있다.

| 바꾸고 싶은 것 | 상수 |
|---|---|
| 부품/화살표/도장/병음/뜻/형광펜 등장 시점 | `PART_A_STAMP_SEC` ~ `HIGHLIGHT_SEC`, `TRAY_SLIDE_SEC` |
| B 구간 전체 길이(현재 10초) | `COMPOSE_B_TOTAL_SEC` |
| A 화면(미리보기/복습) 유지 시간 | `COMPOSE_PREVIEW_HOLD_SEC` / `COMPOSE_REVIEW_HOLD_SEC` |
| 색(타일/도장/형광펜) | `TILE_BG_COLOR`, `SEAL_COLOR`, `HIGHLIGHT_COLOR` |
| 배경(영상/이미지) | 일반 모드와 동일하게 배치 편집기의 "배경 설정" 버튼(`layout.background_value`) — 조합형도 이제 그대로 반영됨 |
| 헤더 문구("왜 A = B?") | `_draw_word_scene()` 안 `header_text = f"왜 ... ?"` |
| 파티클 개수·색 | `PARTICLE_COUNT`, `PARTICLE_PALETTE` |
| 결과 카드 사진 크기·여백 | `RESULT_IMAGE_SIZE`, `RESULT_CARD_PAD`, `RESULT_CARD_GAP`, `RESULT_CARD_ROW_GAP` (사진 없으면 자동으로 한자만 있는 카드로 폴백) |
| 도장 바운스/회전 강도 | `stamp_bounce_scale()` / `stamp_bounce_rotation_deg()` |
| 효과음 파일(block1/2·brush·open) | `word_memorize_compose.py`의 `WORD_MEMORIZE_COMPOSE_*_SOUND_REL` — 재생 트리거는 `word_memorize.py`의 `_sync_compose_sounds()` |
| TTS 등장 시점(부품1/부품2/결과 = 0.5/1.8/3.0초)·겹침 방지 큐 | `word_memorize.py`의 `_begin_compose_word()`(큐 구성) / `_sync_compose_narration()`(재생 순서) |

값을 바꾼 뒤엔 코드만 저장하면 되고(빌드 단계 없음), 재생 시 바로 반영된다.

---

## 3. 자주 헷갈리는 것

- **박스 좌표를 편집기에서 옮겨도 소용없음** — 조합형은 표준 박스 그리드를 아예
  그리지 않고 전용 씬(`ComposeSceneRenderer`)이 화면 전체를 직접 그린다.
  의미 있는 건 `boxes` 순서(=B1/B2/B3)와 word_id 뿐.
- **부품 이미지가 없으면 텍스트만 나옴** — `店`/`电`처럼 개별 이미지가 없는
  한자는 자동으로 텍스트 중앙 배치로 폴백(정상 동작, 에러 아님).
- **효과음** — 부품 타일 등장(block1/block2 랜덤)·획 그어짐(brush)·뜻 팝업(open)은
  연결됨. 도장 폭발·복습 피아노음은 아직 없음(`조합형_need.md` §1 참고).
- **TTS는 부품1·부품2·결과 단어 전부 재생됨** — 순서대로 큐잉되며, 특정 단어에
  ko/zh TTS가 없으면 그 항목만 조용히 건너뛰고 나머지는 그대로 진행(에러 아님).
- **결과 단어의 img_path/meaning은 words.csv 기준** — xlsx만 고치고 CSV를
  재생성 안 하면 반영 안 됨.

---

*관련 문서: `shorts_plan.md`(기획), `조합형_need.md`(부족한 리소스·다음 작업).*
