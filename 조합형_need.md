# 조합형(3번 타입) — 필요 리소스 · 남은 작업 정리

`shorts_plan.md` 기획 + 1단계 구현(코드) 완료 후, 사람이 준비해야 하는 것과
다음 단계에서 다듬을 것을 정리한 문서. 구현 상세는
`studio/studios/word_memorize_compose.py` / `word_memorize.py` 참고.

---

## 1. 지금 당장 필요한 리소스 (에셋)

### 효과음 (3/5 연결 완료 — shorts_plan.md §7 기준)

| 시점 | 필요 효과음 | 현재 상태 |
|---|---|---|
| 부품 타일 등장 | 낮은 "탁" (도장 찍는 느낌) | **연결됨** — `block1.mp3`/`block2.mp3` 랜덤 재생 |
| 획 그어짐 | 붓 "스윽" | **연결됨** — `brush.mp3` |
| **결과 도장 폭발** | **무거운 "쾅" + 반향** | 미연결 — `resource/sound/effect/hamer/hamer1~6.mp3`로 대체 가능 |
| 뜻 팝업 | 맑은 "띵" 1음 | **연결됨** — `open.mp3` |
| 복습 등장 | 피아노 단음 하행 3개 | 없음 — 에셋도 아직 없음 |

연결된 3개는 `word_memorize_compose.py`의 `WORD_MEMORIZE_COMPOSE_*_SOUND_REL` 경로 +
`word_memorize.py`의 `_sync_compose_sounds()`(타이머가 `PART_A_STAMP_SEC`/
`PART_B_STAMP_SEC`/`ARROW_POP_SEC`/`MEANING_POP_SEC`을 지날 때 1회씩 재생, 채널
0/1/7)로 구현. 도장 폭발도 같은 패턴(`_HAMER_EFFECT_CHANNEL` 재사용)으로 붙이면 됨.

### TTS (부품1 → 부품2 → 결과, 각각 한국어 뜻 → 중국어 단어) — 연결 완료

원래 설계는 "조합형은 TTS 없이 고정 타이밍 애니메이션만"이었는데(shorts_plan.md §7이
효과음만 다루고 내레이션이 없었음), 요청에 따라 추가함. 부품1(0.5초 등장)·부품2(1.8초
등장)·결과 단어(3.0초 임팩트) 순서로 각각 "한국어 뜻 → (0.8초 겹쳐) 중국어 단어" TTS를
재생한다 — 다른 타입(ko 모드)과 동일한 순서·겹침 로직 재사용.

**겹침 방지 큐**: 부품 한자는 뜻(2음절 이상인 경우도 있음)이 길면 앞 항목 TTS가
자기 시각적 등장 시점을 지나서도 안 끝날 수 있다(예시 데이터 중 电의 한국어 뜻
TTS가 2.25초). 그래서 각 항목은 "자기 등장 시점"과 "이전 항목 TTS가 끝난 시점"
중 늦은 쪽에서 시작하도록 큐잉했다(`_sync_compose_narration`) — 두 사람이 동시에
말하듯 겹치는 일은 없지만, 그만큼 뒤 항목(특히 결과 단어) 내레이션이 임팩트
순간보다 늦게 시작될 수 있음. 부품 중 TTS가 아예 없는 단어(예시의 冰)는 조용히
건너뛴다. 10초 고정 애니메이션 타이밍 자체는 안 바꿨으므로, 누적 지연이 너무 크면
결과 단어 내레이션이 다음 B로 넘어간 뒤에도 이어질 수 있음 — 부품 뜻이 긴
세트는 실제 재생으로 확인 권장.

TTS 파일은 기존 타입과 동일한 경로(`resolve_vocab_meaning_ko_audio_path`,
`resolve_word_memorize_zh_audio_path`)를 그대로 쓰므로, 부품이든 결과든 이미 다른
배치에서 TTS를 생성해 둔 단어라면 조합형에서도 별도 생성 없이 바로 재생된다.
없으면 `tools/tts_gen/build_word_memorize_tts.py`로 생성.

### 배경음악

레이아웃(`조합_예시1.json`)에 `bg_music_path`를 비워둬서 재생 시 `bg_short` 폴더에서
랜덤 선택됨(기존 타입과 동일 동작). 조합형 전용 BGM을 쓰고 싶으면 해당 mp3를
`resource/sound/bg_short/`에 넣고 레이아웃의 `bg_music_path`를 지정.

### 배경 이미지

`resource/BG/combo_indigo.png` (1080×1920, 다크 인디고 + 중앙 그라디언트)를
코드로 생성해 이미 만들어 둠. **단, `resource/BG/`는 `.gitignore`로 제외되는
경로라 이 세션에서 만든 파일은 git에 커밋되지 않음.** 별도 에셋 배포 방식(클라우드
동기화 등)을 쓰고 있다면 그쪽에도 이 파일을 추가해야 실제 배포 환경에서 보임.

### 이모지 → 실사진으로 대체함 (해결)

shorts_plan.md 목업에는 단어 옆에 이모지(💻🚂🍗)가 있었지만, 확인해보니
이 프로젝트는 이미 `words.xlsx`의 `img_path` + `resource/image/word/*.png`
(단어별 실사진/일러스트) 체계를 다른 타입에서도 쓰고 있었음. 이모지 대신
**기존 이미지 체계를 그대로 재사용**하도록 구현 완료:
- 부품 타일(书/店/电/灯/冰/水)과 A 미리보기 화면 각 줄에 `img_path` 이미지를 표시.
- 이미지가 없는 단어(店, 电 등 — 이번 예시 6개 부품 중 2개는 개별 이미지 없음)는
  자동으로 기존처럼 텍스트만 중앙 배치로 폴백.
- **결과 카드 재설계** (추가 요청 반영, 2차) — 처음엔 도장 옆에 사진을 따로
  붙이는 정도였는데, "더 크게 / 한 카드에 좌측 사진·우측 한자 / 병음·뜻을 카드
  안에 눈에 띄게 합쳐서"라는 요청으로 아예 하나의 큰 카드로 재설계함:
  - 카드 왼쪽=사진(`RESULT_IMAGE_SIZE=230`, 기존 150에서 확대), 오른쪽=한자
    (폰트 84pt→120pt로 확대), 카드 아래쪽에 병음·뜻을 붙여서 표시.
  - 병음·뜻 구역 뒤에 반투명 어두운 배경 띠를 깔아 대비 확보 — 안 넣었더니
    성조색 1성(빨강)이 도장 배경(빨강 계열)에 묻혀 잘 안 보이는 문제가 있었음.
  - 임팩트 시 카드 전체(사진+한자+병음+뜻 다)가 하나의 서피스로 합성돼 같은
    바운스·회전(`stamp_bounce_scale`/`stamp_bounce_rotation_deg`)을 받음 — 병음·뜻은
    각자 등장 시점(`PINYIN_POP_SEC`/`MEANING_POP_SEC`, 바운스가 끝난 뒤라 시점상 자연스러움)에
    맞춰 페이드인. 사진이 없는 결과 단어는 자동으로 한자만 있는 카드로 폴백.
  - **(해결됨) 병음·뜻 등장 시 카드 크기가 바뀌던 버그** — 처음엔 병음·뜻이
    없을 때(`show_pinyin`/`show_meaning`=False) 그 두 줄 자체를 안 만들어서
    카드 높이가 작았다가, 등장 시점에 그 줄이 추가되며 카드가 커지는 문제가
    있었음(재생해보니 사진+한자 뜬 직후 병음 뜰 때 카드가 커 보임). 지금은
    병음·뜻 서피스를 **항상 만들어서 카드 크기를 고정**하고, 안 보일 때는
    알파값만 0으로 둠 — 임팩트 시점부터 카드 크기가 끝까지 동일함.

**남은 이슈**: 새 조합형 세트를 만들 때 부품 한자에 이미지가 없으면 카드가
휑해 보일 수 있음 — 부품용 이미지도 같이 준비하거나, 폴백 디자인을 더
다듬을지 결정 필요.

---

## 2. 콘텐츠 제작(스케일업) — 자동화 완료

`tools/words_batch/build_compose_layout.py` + `.words_compose` 파일 포맷으로
자동화함(`word-memorize-batch` 스킬 §4에 반영). 새 조합형 세트 만드는 절차:

1. 부품 한자·결과 합성어가 `words.xlsx`에 이미 있는지 확인. 없으면 평소처럼
   `.words_add` + `python -m tools.words_batch.add_words`로 추가.
2. `.words_compose` 파일(3열: `result  component1  component2`, 전부 한자 텍스트)
   작성 후 `python -m tools.words_batch.build_compose_layout ... --like 조합_예시1.json --name <이름>`
   한 번 실행 — `component1_id`/`component2_id` 연결, CSV 재생성, `combo_layout: true`
   레이아웃 JSON 저장까지 한 번에 처리됨. `--dry-run`으로 먼저 미리보기 가능.

**남은 것**: 이 스크립트는 "연결"만 하고 부품 한자 자체는 안 만든다 — 완전히
새로운 한자(예: 처음 쓰는 부품)는 여전히 1번(`.words_add`)을 거쳐야 함.
`.words_compose` 자체에 부품의 meaning/pinyin까지 넣어 한 파일로 신규 단어 추가부터
연결까지 다 하게 만드는 건 추가로 할 수 있음(현재는 2단계로 분리돼 있음).

---

## 3. 남은 시각/타이밍 다듬기 (선택)

- 부품 타일 크기·위치, 헤더 문구("왜 A = B?") 톤 등은 실제 재생 화면을 보고
  미세 조정 필요.
- 결과 도장 바운스/쉐이크/파티클 강도는 초기값이라 실제 영상으로 보면서 조정 여지 있음.

### (해결됨) 실제 재생 시 부품 타일·이미지가 안 보이던 버그

"조합 예시 재생이 잘 안 나온다"는 리포트를 받고, `table_editor` GUI가 실제로
띄우는 것과 동일한 명령(`python -m studio.runner --mode debug --studio word_memorize
--layout ... `)을 헤드리스로 그대로 재현해서 잡은 버그 2건 — 둘 다
**헤드리스 단위 테스트에서는 안 드러나고 실제 실행 경로에서만 터지는 유형**이라 발견이 늦었음.

1. **폰트 전부 조용히 실패** — `ComposeSceneRenderer.__init__`이 생성자에서 바로
   폰트를 로드했는데, `WordMemorizeStudio`는 `runner.main()`에서 `pygame.init()`
   *이전*(`_create_studio()` 시점)에 생성됨 → pygame.font가 아직 없어 모든 폰트
   로드가 조용히 실패(`try/except`로 삼켜짐) → 텍스트가 전혀 안 그려짐.
   기존 `WordMemorizeRenderer`가 왜 `ensure_fonts()`를 `draw()` 시점까지 미루는지
   그 이유였음 — `ComposeSceneRenderer`도 동일하게 지연 로딩으로 수정.
2. **부품 단어를 못 찾음** — `_words_by_id_for_draw()`가 레이아웃 `boxes`에 있는
   단어(=결과 합성어 3개)만 담고 부품 단어(书/店/电/灯/冰/水)는 전혀 포함하지
   않음 → 부품 타일이 조용히 스킵됨. `layout_uses_compose`일 때 부품 단어도
   `data.table_manager.get_word()`로 채워 넣도록 수정.

두 버그 다 고친 뒤 동일한 재현 스크립트로 재검증 완료(부품 타일·이미지·병음·
형광펜 전부 정상 표시), 기존 레이저/토픽 타입도 회귀 없음 확인.

---

*작성: 1단계 코드 구현 완료 시점 기준.*
